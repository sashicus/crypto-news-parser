name: Run Crypto Parser

on:
  push:
    branches:
      - main  # Запуск при пуше в main
  schedule:
    - cron: '0 * * * *'  # Запуск каждый час

jobs:
  run_script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout репозиторий
        uses: actions/checkout@v4

      - name: Установка Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Установка зависимостей
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Проверка переменных окружения
        run: |
          echo "HF_API_KEY=${{ secrets.HUGGING_FACE_API_KEY }}" >> $GITHUB_ENV
          echo "BOT_TOKEN=${{ secrets.BOT_TOKEN }}" >> $GITHUB_ENV
          echo "CHAT_ID=${{ secrets.CHAT_ID }}" >> $GITHUB_ENV
          echo "🔍 Переменные окружения загружены"

      - name: Запуск парсера
        env:
          HF_API_KEY: ${{ secrets.HUGGING_FACE_API_KEY }}
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
        run: python parser.py

      - name: Логи
        run: echo "✅ Парсер завершил работу"
